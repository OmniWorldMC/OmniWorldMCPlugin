package com.omniworldmc.plugin.util;

import org.bukkit.ChatColor;

public class Info {
    public static final String CHAT_PREFIX = ChatColor.WHITE + "[" + ChatColor.AQUA + "OmniWorld" + ChatColor.GOLD + "MC" + ChatColor.WHITE + "]" + ChatColor.GREEN;
    public static final String NO_PERMS = ChatColor.WHITE + "[" + ChatColor.AQUA + "OmniWorld" + ChatColor.GOLD + "MC" + ChatColor.WHITE + "]" + ChatColor.RED + "You don't have permission to use that command!";
}
